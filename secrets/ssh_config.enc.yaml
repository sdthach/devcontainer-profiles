# =============================================================================
# SSH Config — SOPS-encrypted
# This is a placeholder. To encrypt the real config:
#   sops --encrypt --input-type yaml --output-type yaml config/ssh_config.template > secrets/ssh_config.enc.yaml
# Decrypted at container creation by scripts/sync-secrets.sh
# =============================================================================
# Run: sops secrets/ssh_config.enc.yaml
# After you set up your AGE key and update .sops.yaml with your public key.
